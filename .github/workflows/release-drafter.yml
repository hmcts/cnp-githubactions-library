# This is an example workflow for using the Release Drafter action and the update-changelog action together to automatically draft a release and update the changelog file.
name: Release Drafter

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  draft:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version: ${{ steps.drafter.outputs.resolved_version }}
      tag:     ${{ steps.drafter.outputs.tag_name }}
    steps:
      - uses: actions/checkout@v4

      - uses: release-drafter/release-drafter@v6
        id: drafter
        env:
          GITHUB_TOKEN: ${{ github.token }}

  changelog:
    needs: draft
    if: needs.draft.outputs.version != ''
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: ./update-changelog
        with:
          github-token: ${{ github.token }}
          version: ${{ needs.draft.outputs.version }}
          tag:     ${{ needs.draft.outputs.tag }}
